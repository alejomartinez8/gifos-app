// Variables
const inputSearch = document.querySelector(".input-search");

// Functions
function handleInputSearch(e) {
	console.log(e);
}

// Events
inputSearch.addEventListener("keypress", handleInputSearch);
